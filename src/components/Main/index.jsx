import React from "react";
import * as S from "./styles";
import Card from './../Card'

const Main = ({ title = "Boilerplate" }) => {
  return (
    <main>
      
      <Card/>
    </main>
  );
};

export default Main;
